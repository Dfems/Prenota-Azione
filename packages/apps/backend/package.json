{
  "name": "@prenota-azione/backend",
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "start:dev": "nest start --watch",
    "build": "nest build",
    "lint": "eslint --ext .ts src",
    "test": "jest",
    "clean": "rimraf dist",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:seed": "prisma db seed"
  },
  "dependencies": {
    "@nestjs/common": "^9.0.0",
    "@nestjs/core": "^9.0.0",
    "@nestjs/platform-express": "^9.0.0",
    "reflect-metadata": "^0.1.13",
    "rxjs": "^7.0.0",
    "@prisma/client": "^4.0.0"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.0",
    "@nestjs/schematics": "^11.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3",
    "prisma": "^6.6.0"
  }
}
